import { Container } from "@/components/Container";
import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <Container>
      <div class="mt-12">One more time</div>
    </Container>
  );
});
